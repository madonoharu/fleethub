description = "PT命中テスト9"

# 攻撃艦設定
[attacker]
ship_id = "由良改二"
g1 = "SG レーダー(後期型)"
g2 = { gear_id = "二式水戦改", stars = 0, exp = 0 }

# 防御艦設定
[target]
ship_id = 1638

# 昼戦単発
[expected.day.data.SingleAttack]
# 命中率（通常命中率 + クリティカル率）
hit_rate.total = 0.14
# クリティカル率
hit_rate.critical = 0.05


# 命中項
hit_rate_params.accuracy_term = 93
# 回避項
hit_rate_params.evasion_term = 80
