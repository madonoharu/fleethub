{
  "name": "kcs",
  "scripts": {
    "build": "yarn esbuild src/index.ts --bundle --minify --outfile=lib/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.14.28",
    "fleethub-core": "npm:latest"
  }
}
